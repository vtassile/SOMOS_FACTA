import { DataFilterPipePipe } from './data-filter-pipe.pipe';

describe('DataFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
