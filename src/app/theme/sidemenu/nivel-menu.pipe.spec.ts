import { NivelMenuPipe } from './nivel-menu.pipe';

describe('NivelMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new NivelMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
